---
description: 自增自减运算符
tabTrigger:  gra-style 11 increment decrement
scope:       source.js
---
/**
 * 建议: 自增和自减运算符 用 += 和 -= 替代
 * 自增（++）和自减（--）运算符，放在变量的前面或后面，返回的值不一样，很容易发生错误。事实上，所有的++运算符都可以用+= 1代替。
 */
++x
// 等同于
x += 1;
